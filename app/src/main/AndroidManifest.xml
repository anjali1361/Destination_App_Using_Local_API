<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anjali.destinationlistappusingretrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DestinationListAppUsingRetrofit">
        <activity android:name=".activities.DestinationDetailActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="Destination Detail"/>

        <activity
            android:name=".activities.DestinationCreateActivity"
            android:label="New Destination"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.DestinationListActivity"
            android:label="Destination List"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.WelcomeActivity"
            android:label="GloboFly"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>